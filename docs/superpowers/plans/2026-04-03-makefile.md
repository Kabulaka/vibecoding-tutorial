# Makefile Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在仓库根目录新增一个常用版 `Makefile`，统一 mdBook 的构建、预览、清理、检查和打开首页入口命令。

**Architecture:** 采用单文件薄封装方案，在仓库根目录新增 `Makefile`，只暴露 `build`、`serve`、`clean`、`check`、`open` 五个目标。`check` 通过 `mdbook build` + Python 文件存在性校验来验证关键输出，`open` 以“打印路径优先、系统支持时尝试打开”为原则保持轻量兼容。

**Tech Stack:** Make, mdBook, Python 3, POSIX shell

---

## File Structure

- Create: `Makefile` — 仓库根目录的常用入口命令，封装 mdBook 工作流

### Task 1: 新增 Makefile 常量与基础目标

**Files:**
- Create: `Makefile`

- [ ] **Step 1: 确认仓库根目录当前没有 Makefile，避免覆盖现有入口**

Run:
```bash
cd /home/nika/workspace/doc/vibecoding && ls Makefile makefile GNUmakefile 2>/dev/null || true
```

Expected: PASS，无输出，表示当前没有现成 Makefile。

- [ ] **Step 2: 创建 `Makefile` 的基础结构、默认目标与路径变量**

Write `Makefile`:
```make
.DEFAULT_GOAL := build

DIST_DIR := dist/vibecoding-tutorial-book
INDEX_HTML := $(DIST_DIR)/index.html

.PHONY: build serve clean check open
```

- [ ] **Step 3: 添加 `build`、`serve`、`clean` 三个基础目标**

Append to `Makefile`:
```make
build:
	mdbook build

serve:
	mdbook serve

clean:
	rm -rf $(DIST_DIR)
```

- [ ] **Step 4: 运行 `make build`，确认基础构建目标可用**

Run:
```bash
cd /home/nika/workspace/doc/vibecoding && make build
```

Expected: PASS，输出包含 `HTML book written to` 且目标目录为 `dist/vibecoding-tutorial-book`。

### Task 2: 添加 `check` 目标做关键产物校验

**Files:**
- Modify: `Makefile`

- [ ] **Step 1: 添加 `check` 目标，先执行构建再校验关键 HTML 文件存在**

Append to `Makefile`:
```make
check:
	mdbook build
	python3 - <<'PY'
	from pathlib import Path
	root = Path('dist/vibecoding-tutorial-book')
	required = [
	    root / 'index.html',
	    root / 'quick-start/index.html',
	    root / 'core-concepts/index.html',
	    root / 'practical-skills/index.html',
	    root / 'case-study/index.html',
	    root / 'best-practices/index.html',
	]
	missing = [str(path) for path in required if not path.exists()]
	if missing:
	    raise SystemExit('Missing files: ' + ', '.join(missing))
	print('OK: all required html files exist')
	PY
```

- [ ] **Step 2: 运行 `make check`，确认检查目标成功且输出明确通过信息**

Run:
```bash
cd /home/nika/workspace/doc/vibecoding && make check
```

Expected: PASS，输出包含 `OK: all required html files exist`。

- [ ] **Step 3: 确认 `check` 没有引入脆弱的导航文本断言**

Read `Makefile` and verify the `check` target only checks build success plus required HTML existence, not exact sidebar label text.

Expected: PASS，仅包含构建和文件存在性校验逻辑。

### Task 3: 添加 `open` 目标并完成整体验收

**Files:**
- Modify: `Makefile`

- [ ] **Step 1: 添加 `open` 目标，打印首页路径并按系统能力尝试打开**

Append to `Makefile`:
```make
open:
	@printf '%s\n' "$(INDEX_HTML)"
	@if command -v xdg-open >/dev/null 2>&1; then \
		xdg-open "$(INDEX_HTML)"; \
	elif command -v open >/dev/null 2>&1; then \
		open "$(INDEX_HTML)"; \
	else \
		printf '%s\n' 'No supported open command found; printed path only.'; \
	fi
```

- [ ] **Step 2: 运行 `make open`，确认至少打印入口页路径**

Run:
```bash
cd /home/nika/workspace/doc/vibecoding && make open
```

Expected: PASS，输出至少包含 `dist/vibecoding-tutorial-book/index.html`；如系统支持，还会尝试打开文件。

- [ ] **Step 3: 运行 `make clean`，确认构建目录被删除**

Run:
```bash
cd /home/nika/workspace/doc/vibecoding && make clean && test ! -d dist/vibecoding-tutorial-book
```

Expected: PASS，无输出且退出码为 0。

- [ ] **Step 4: 再次运行 `make check`，确认清理后仍可重新构建并通过检查**

Run:
```bash
cd /home/nika/workspace/doc/vibecoding && make check
```

Expected: PASS，输出包含 `OK: all required html files exist`。

- [ ] **Step 5: 查看最终工作树状态，确认只包含本次计划内改动**

Run:
```bash
cd /home/nika/workspace/doc/vibecoding && git status --short
```

Expected: 只出现 `Makefile` 以及可能的 `dist/` 构建产物变更。

## Self-Review Checklist

- Spec coverage:
  - `build`：Task 1 覆盖
  - `serve`：Task 1 覆盖
  - `clean`：Task 1 / Task 3 覆盖
  - `check`：Task 2 覆盖
  - `open`：Task 3 覆盖
- Placeholder scan: 无 `TODO` / `TBD` / “后续补充” 类占位内容
- Type consistency: 所有输出目录和入口文件路径统一为 `dist/vibecoding-tutorial-book` 与 `dist/vibecoding-tutorial-book/index.html`
